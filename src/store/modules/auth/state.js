// import _ from "lodash";
export default {
    accessToken: null,
    refreshToken: null,
    APIData: '',
    // isLoggedIn: JSON.parse(window.localStorage.getItem("isLoggedIn")),
    // permissions: _.split(window.localStorage.getItem("permissions"), ",")
};